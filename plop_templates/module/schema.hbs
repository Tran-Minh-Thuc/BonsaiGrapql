import { gql } from "apollo-server"
import { BaseDocument } from "../../base/baseModel"

declare global {
    type {{properCase name}} = BaseDocument & {
        _id?: string,
        //testId?: string,
        name?: string,
    }
}

export default gql`
    # TYPE
    type {{properCase name}} {
        _id: ID
        #testId: ID
        name: String

        #test:Test
        createdAt: Float
        updatedAt: Float
    }

        type Result {
        code: String
        status: String
        mesage: String
        data: {{properCase name}}
    }

    #ROOT TYPE
    type Query {
        getAll{{properCase name}}(q: QueryInput): {{properCase name}}PageData
        getOne{{properCase name}}(_id: ID!): Result
    }

    type {{properCase name}}PageData {
        data: [{{properCase name}}]
        pagination: Pagination
    }

    type Mutation {
        create{{properCase name}}(data: Create{{properCase name}}Input): Result
        update{{properCase name}}(_id: ID!, data: Update{{properCase name}}Input): Result
        delete{{properCase name}}(_id: ID!): Result
    }

    # INPUT
    input Create{{properCase name}}Input {
        name: String!
    }

    input Update{{properCase name}}Input {
        name: String
    }
`