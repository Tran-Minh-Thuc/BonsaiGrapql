import { gql } from "apollo-server"
import { BaseDocument } from "../../base/baseModel"

declare global {
    type {{properCase name}} = BaseDocument & {
        _id?: string,
        name?: string,
    }
}

export default gql`
    # TYPE
    type {{properCase name}} {
        _id: ID
        name: String

        createdAt: Float
        updatedAt: Float
    }

    #ROOT TYPE
    type Query {
        getAll{{properCase name}}(q: QueryInput): {{properCase name}}PageData
        getOne{{properCase name}}(_id: ID!): {{properCase name}}
    }

    type {{properCase name}}PageData {
        data: [{{properCase name}}]
        pagination: Pagination
    }

    type Mutation {
        create{{properCase name}}(data: Create{{properCase name}}Input): {{properCase name}}
        update{{properCase name}}(_id: ID!, data: Update{{properCase name}}Input): {{properCase name}}
        delete{{properCase name}}(_id: ID!): {{properCase name}}
    }

    # INPUT
    input Create{{properCase name}}Input {
        name: String!
    }

    input Update{{properCase name}}Input {
        name: String
    }
`